# Simple and modern omok game with python

Simple Omok game. 

# Requriements

Install `pygame` via pip. You also need `tkinter`. 

# Recommend settings

Use `venv` in Python.

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install pygame
```




